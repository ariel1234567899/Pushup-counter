{
  "name": "push-up-tracker-notifications",
  "private": true,
  "type": "module",
  "scripts": { "dev": "wrangler dev --test-scheduled", "deploy": "wrangler deploy" },
  "dependencies": { "web-push": "^3.6.7" },
  "devDependencies": { "wrangler": "^4.0.0" }
}
